# Ansible

Playbooks and roles for homelab automation.

## Structure

```
├── inventory/        # Host inventory files
├── playbooks/        # Playbooks
├── roles/            # Reusable roles
└── group_vars/       # Group variables (secrets in private/)
```
